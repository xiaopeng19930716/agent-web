# 2026-08-24

## 后端热重启
- 给后端加了 nodemon 热重载。新增 `server:watch` 脚本(`nodemon --watch server --ext js,json --ignore server/data --ignore server/node_modules server/index.js`),`dev:all` 中的 `server` 改为 `server:watch`。
- `--ignore server/data` 避免持久化 JSON 频繁写入导致死循环重启。改后端文件后自动重启,无需手动。nodemon 已加入 devDependencies(`^3.1.14`)。

## 会话归档过滤接口
- `GET /api/sessions` 新增 `archived` 查询参数:空/缺省=全部,`'0'`=未归档,`'1'`=已归档。session 模型已有 `archived` 布尔字段(软删除,数据保留在 sessions.json)。
- 前端 `fetchSessions(archived)` 支持该参数;`App.vue` 与 `ChatPanel.vue` 初始化时调用 `fetchSessions(0)`,左侧对话框仅显示未归档会话。

## 编辑规范
- 绝对不要对 `.vue` SFC 使用 `write_to_file` 整段覆盖——会把 `<template>`/`<style>`/import 全部丢掉,Vite 会报 `At least one <template> or <script> is required`。`src/sessions.js` 这类纯 JS 模块用 `write_to_file` 是安全的。
- 修改 SFC 时只用 `replace_in_file` 精准改;如已误覆盖,用 `git checkout -- <file>` 恢复后再改。

## 无项目文件操作策略（A 方案）
- 用户决定维持 A：无项目=纯聊天/MCP，不能操作本地文件（chat.js:61 `projectRoot ? perm : 'none'` 不变，不做 B 沙箱目录）。
- 体验优化：chat.js `buildProjectSection` 无项目分支补充提示语——agent 被要求操作文件时应告知「通用对话模式未关联项目无法操作本地文件」并引导用户先关联项目。
- 配合 ComposerInput.vue UI 优化：无项目时直接隐藏 `@` 按钮和权限下拉（`v-if="activeProjectId.id"`），不再用灰显"沙箱"占位——更干净，避免误导。模型/强度/`/` 工具按钮/发送与项目无关，始终保留。

## #2 停止生成（AbortController）已落地
- 后端 chat.js: runAgent 接 abortSignal(循环首/工具前检查)、streamModelTurn 传 signal 给 model.stream、executeToolCall 执行前/ask 后检查 abortSignal 发 canceled 事件。
- routes/chat.js: 新增 abortControllers Map + POST /chat/abort; /chat 内建 AbortController(per sessionId), res.on('close') 时 abort(浏览器断流即停), 传 abortSignal 给 runAgent。
- 前端 api/agent.js 新增 abortChat(); ChatPanel.vue 加 stopGeneration() 调 abortChat + loading=false, 模板 ComposerInput 加 @stop; ComposerInput.vue 发送按钮 loading 时变 Square 停止图标, emit('stop')。

## 工具确认流（#1 approve）已落地
- 权限下拉(ComposerInput.vue)新增 `ask` 选项;无项目时下拉收敛为一项灰显"沙箱(无项目)"(A2 方案),用 `permissionOptions` computed + `activeProjectId`。
- fileTools.js 给每个工具加 `risk` 字段(read/write/danger)。
- chat.js: `buildPermissionSection` 加 `ask` 分支; `executeToolCall` 加确认闸门——ask 模式 + 高风险(write/danger)工具先发 `tool_confirm` 事件并 await gate.ask();危险命令(rm -rf 等)强制拒绝; `runAgent` 透传 `confirmGate`。
- routes/chat.js: 新增 `ConfirmGate` 类(按 sessionId 存 Map,超时5分钟自动 deny,客户端断开 close() 全部 deny) + `POST /chat/confirm` 路由; `perm==='ask' && projectRoot` 时建 gate 传入。
- 前端: api/agent.js 的 streamChat 解析 `tool_confirm` 事件(onToolConfirm) + 新增 confirmToolCall(); ChatPanel.vue 渲染 a-modal 确认弹窗,允许/拒绝回传 /api/chat/confirm。streamChat 已带 sessionId。
