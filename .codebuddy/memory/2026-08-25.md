# 2026-08-25

## 工具重试按钮 (#10) 收尾
- 重试按钮从圆形 Redo2 图标改为内联红色文字「重试」，仅失败工具显示（isToolFailed 正则），置于工具卡片头部状态标签「完成」左侧（用户要求「完成按钮的左边」）。
- 修复：失败工具状态标签原本错误显示「完成」→ 改为根据 isToolFailed 显示红色「失败」（新增 .timeline__status--failed 样式）。
- 修复 Less 编译报错「Unrecognised input. Possibly missing opening '{'」：编辑 .timeline__result-body 时 old_str 只覆盖块前半，导致 font-size/max-height 等属性脱离选择器成裸属性；已并回完整块。

## 功能清单盘点（存入 MEMORY.md）
- 用户发来完整 10+ 项功能清单与进度。核对代码后：#7 todo、#10 工具重试实际已完成（与用户「未做」标注不符）；#1/#2/#8/#13/#14/#16 已完成。
- 真正未做的高优先级：#3 编辑历史消息重试、#4 代码块应用到文件、#5 导出对话、#6 多Agent规划、#9 图像理解、#11 SSE重连、#12 用量看板、#13b 代理配置。
- 建议顺序已写入 MEMORY.md。下一步待用户确认做哪个（用户原建议 #6，但 #10/#7 实际已完）。

## #12 用量统计页面（新建）
- 用户要求独立页面（非设置内）+ 图表展示，安装 echarts。路由 `/usage`，侧边栏 footer 设置上方加「用量」入口。
- UsageStats.vue：前端聚合 fetchSessions 全部会话 assistant metadata；调用次数含失败轮次；按时间段（全部/今天/7天/30天）过滤。
- 迭代：用户最终要求——① 调用次数图改为**按厂商 vendorKey 聚合**（非按模型）；② 各模型 Token 图 y 轴标签截断优化(width:140 + tooltip 完整)；③ 去掉顶部三张汇总卡片。均已改完。

## #12 用量页面二次迭代（图表与表格细节）
- 厂商聚合修复：metadata.model 可能是纯 modelId（如 qwen3.7-max-2026-05-17）或组合键（Alibaba/qwen3.7-...）。新建 `modelToVendorMap`（modelId→厂商名，遍历 settings.vendors）权威解析厂商；纯 modelId 直接查 map，组合键取 modelId 查 map；vendorRows 按**厂商名**聚合（之前按 vendorKey 导致拆条）。
- Token 图：横向改竖向柱状（x 轴 modelId，y 轴用量）；y 轴/label/tooltip 按 **M(百万) 统计，toFixed(4)**；x 轴标签平放不旋转、重叠截断 width:90。
- tooltip 顺序：`模型ID / 厂商`；各模型 Token 数量和单位 M。
- 表格：四列 模型(modelId) / 厂商(vendor, 无则空) / Token总数(千分号) / 调用次数；Token 列放调用次数前且更宽，模型/厂商列窄+ellipsis。
- 厂商调用次数柱状图 barWidth:20。

## 进度最终对齐（2026-08-25 末）
- 已完成：#1 #2 #3 #7 #8 #10 #12 #13 #14 #16（#3 用户确认含编辑重试；#12 即本页）。
- 暂搁置：#4 #5。 剩余未做：#6 #9 #11 #13b。
- MEMORY.md 已整体更新到该状态。

## 下午：暗色模式全局修复 + #11 + #9 + 规范确立
- 用户发现设置页/MCP/Skills 暗色下仍是浅色。根因三连：① antdv 没套 ConfigProvider+darkAlgorithm；② tailwind.config.js 缺 darkMode:'class'（默认 media，dark: 不生效）；③ .app 根背景用 Less 变量 @color-bg（编译期固定浅色）。全部修复。
- 确立**颜色强制规范**：禁止硬编码颜色（Tailwind 固定浅色类 / 字面量如 #1f1f1f），统一用 theme.css 的 CSS 变量（--color-bg 等），已写入 MEMORY.md 长期记忆。
- 暗色主色调为深蓝灰（--color-bg:#0f172a / --color-bg-subtle:#1e293b / --color-border:#334155），非纯黑；antdv darkAlgorithm 的 colorBgContainer 也对齐 #1e293b。
- 五个设置页（ModelSettings/SettingsPanel/UsageStats/McpSettings/SkillsSettings）暗色适配全改走变量。
- **#11 SSE 断流重连 + 本地缓冲**（src/api/agent.js streamChat）：runOnce 三态 + 指数退避重连(maxRetries=3) + onReset 清半成品 + ChatPanel 重连提示条。完成。
- **#9 图像/截图理解**：📎上传 + Ctrl+V 粘贴；前端 dataURL → POST /api/upload 落盘 server/.uploads 拿短 URL；user.content 多模态数组；后端 toLangchainMessage 原生兼容零改动；MessageList 渲染缩略图+点击放大。完成。server/.uploads 加 .gitignore。
- #13b 代理配置：用户确认 Electron 打包前后端一体，不需要自定义地址/代理，从清单移除。
- **剩余仅 #6 多 Agent 规划模式（用户说稍后再做）；#4 #5 仍搁置。**
- MEMORY.md 功能清单与实现细节段已整体更新。
