# 2026-08-26

## git 远程切换 SSH 并修复拉取/推送
- 原 HTTPS 直连 GitHub 443 被网络阻断（TCP 连接失败），改用 SSH（端口 22 通）。
- 远程地址：`git@github.com:xiaopeng19930716/agent-web.git`
- 旧 `id_ed25519` 带 passphrase，非交互环境读不到（ssh-agent 需管理员权限启动被拒）。新建**无密码** key：`~/.ssh/id_ed25519_nopass`（公钥 `id_ed25519_nopass.pub` 已加 GitHub）。
- 关键坑：`core.sshCommand` 里不能用 `$env:USERPROFILE`（git 不展开，路径错）。必须用绝对路径：
  `git config core.sshCommand "ssh -i C:/Users/kathi/.ssh/id_ed25519_nopass -o IdentitiesOnly=yes"`
- rebase 冲突：本地「图片理解(images)」与远程「计划模式(planMode)」功能叠加，已合并保留两边（ComposerInput/ChatPanel/MessageList），并清掉残留 `<<<<<<<` 标记。
- 已成功 `git pull --rebase` + `git push origin main`。
