# 2026-08-26

## git 远程切换 SSH 并修复拉取/推送
- 原 HTTPS 直连 GitHub 443 被网络阻断（TCP 连接失败），改用 SSH（端口 22 通）。
- 远程地址：`git@github.com:xiaopeng19930716/agent-web.git`
- 旧 `id_ed25519` 带 passphrase，非交互环境读不到（ssh-agent 需管理员权限启动被拒）。新建**无密码** key：`~/.ssh/id_ed25519_nopass`（公钥 `id_ed25519_nopass.pub` 已加 GitHub）。
- 关键坑：`core.sshCommand` 里不能用 `$env:USERPROFILE`（git 不展开，路径错）。必须用绝对路径：
  `git config core.sshCommand "ssh -i C:/Users/kathi/.ssh/id_ed25519_nopass -o IdentitiesOnly=yes"`
- rebase 冲突：本地「图片理解(images)」与远程「计划模式(planMode)」功能叠加，已合并保留两边（ComposerInput/ChatPanel/MessageList），并清掉残留 `<<<<<<<` 标记。
- 已成功 `git pull --rebase` + `git push origin main`。

## #4 代码块「应用到文件」
- 后端新增 `POST /api/file/write`（server/routes/upload.js），复用 `safeResolve`(fileTools.js, 已 export) + `projects`(store.js) + `permission==='full'` 校验；写前对已存在文件备份到 `项目根/.agent-backup/`。无项目时 root 退回 `os.homedir()`。
- 前端 `src/api/agent.js` 新增 `writeProjectFile(content, relPath, projectId, permission)`。
- `MessageList.vue`：自定义 marked renderer 给每个代码块包 `.code-block[data-ci]`，模块级 `codeBlocks` 数组收集原文（renderMarkdown 时清空重置），`bubble__markdown` 上 `@click` 委托 `.code-apply-btn` 弹出 a-modal 填相对路径后写入；仅当 `props.projectId` 且有 lang 时显示按钮。样式用 `--color-*` 暗色变量。

## #5 导出对话 + 清空对话
- `ChatHeader.vue` 头部加：导出下拉（MD / JSON，纯前端 Blob 下载，用 `s.messages`；图片型 content 取 `[图片]`）+ 清空按钮（`Modal.confirm` 后 emit `clear-chat`）。
- `ChatPanel.vue` 监听 `@clear-chat` 调 `truncateSession(id, 0)` 清空全部消息（不清退文件变更），并 `message` 提示。

## MessageList 消息组件拆分（结构重构）
- 把每条消息渲染从 `MessageList.vue` 拆成独立组件，便于后续单独维护：
  - `src/components/chat/messages/UserMessage.vue`：用户气泡 + 头像 + token chip + 图片预览 + 回退按钮。内部持有 `renderUserHTML`（彩色 chip 渲染）。emit `rollback`。
  - `src/components/chat/messages/AIMessage.vue`：AI 气泡 + 思考区 + 工具时间线 + 计划卡 + 子任务 + markdown + 延迟标签 + 复制/重生成。状态（thinkingExpanded/resultExpanded/retrying/copiedSet）改为组件内本地 ref（不再按索引 i）。emit `regenerate`/`retryTool`/`open-subagent`。
  - `src/components/chat/messages/messageBase.less`：共享气泡/操作条/头像/token-chip/思考/工具/计划/子任务/图片/代码块样式 + keyframes，两组件 `@import`。
- `MessageList.vue` 仅作列表容器：`v-for` 按角色分发 UserMessage/AIMessage；保留 markdown 渲染（marked/highlight.js 配置、`codeBlocks`、`renderMarkdown`、`onMarkdownClick`、`confirmApplyFile`、`guessExt`）、滚动/回到底部、任务清单按钮、`applyModal`。共享逻辑通过 prop 传入 `:markdown-html` 与 `:on-markdown-click`，事件原样转发给 ChatPanel。
- 注意：`renderMarkdown` 每次调用会 `codeBlocks.length=0`，多 AI 消息时仅最后一条代码块「应用到文件」索引正确（与原行为一致，未修）。
- 任务清单按钮保持方形圆角（border-radius:10px），且仅在 `session.todos` 非空时显示。回到底部按钮保持圆形。

## 组件拆分踩坑：消息字段名必须对齐原版
- 拆分时最初臆造了字段名/类名导致两个 bug：思考区消失（字段 `m.thinking` 应为 `m.reasoning`/`m.reasoningDone`）、复制/重生成按钮样式错乱（臆造 `.tool`/`.timeline__*`(结构不同)/`.plan-card`/`.subagent-ref` 等与原版不符）。
- 结论：拆分组件必须严格照搬原版真实字段（reasoning/reasoningDone/toolCalls{name,args,status,result}/subAgentRefs{id,title,status}/plan{id,title,description}/metadata.firstTokenMs）与类名，messageBase.less 直接还原原版样式。
- 工具重试 loading：AIMessage 用 `watch` 监听 `toolCalls.map(t=>t.status|result.length)`，状态变 done 自动清 `retrying`（替代原版 MessageList 的 clearRetrying 转发，MessageList 保留空 clearRetrying 兼容 ChatPanel 的 `?.clearRetrying` 调用避免报错）。

## 思考区/内容区极简化（ui-ux-pro-max）
- AIMessage 思考区改为 `.thinking--clean`：去掉卡片边框/背景/圆角，纯文字细排版（5px 圆点 + 灰色标签「已思考」/「思考中」+ 工具次数摘要），reasoning 用左侧 1px 细分隔线分隔；工具时间线去掉 `⚙` emoji，直接工具名。
- 模板：展开/收起箭头统一用 `thinking__chevron--down`（margin-left:auto 靠右）。

## 思考区 & 工具时间线视觉升级
- 思考区改为卡片式设计：渐变顶条、柔和背景、胶囊状态标签、旋转 Brain 图标；reasoning 正文放在白色内嵌卡片中，使用 `color-mix()` 处理主色透明度以兼容 CSS 变量主题。
- 工具时间线改为卡片式时间轴：每个 toolCall 独立卡片，左侧彩色状态条（主色/绿色/红色），工具名左侧图标按名称关键词映射（Search/FileSearch/Globe/Database/Code/Terminal/Wrench），参数以 key/value chip 展示，结果展开带 chevron 动画，状态徽章带图标。
- 修正 less 编译问题：`@color-primary` 实际为 `var(--color-primary)`，不能用 `fade()`/`red()` 等 less 颜色函数，统一改用 CSS `color-mix(in srgb, var(--color-primary) X%, transparent)`。
